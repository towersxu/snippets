/**
 * 获取循环有序数组中的给定值
 */

let arr = [4, 5, 6, 7, 8, 1, 2, 3]

function CircleSearch (array, val) {
  let left = 0
  let right = array.length - 1
  // 如果左侧的值大于右侧的值。
  // while ()
}

CircleSearch(arr, 2)